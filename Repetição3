package main

import "fmt"

func main() {
	//Ordem dos números ímpares de 1 a 19.
	fmt.Println("A ordem crescente dos números é: ")
	for i := 0; i < 21; i++ {
		if i %2 == 0 {
			continue
		}
		fmt.Println(i)
	}
}
